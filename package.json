{
  "name": "ts-app-directory-template",
  "private": true,
  "workspaces": [
    "frontend",
    "packages"
  ],
  "scripts": {
    "client:dev": "npm run dev --workspace=frontend",
    "client:build": "npm run build --workspace=frontend",
    "client:lint": "npm run lint --workspace=frontend"
  }
}
